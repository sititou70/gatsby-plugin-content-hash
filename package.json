{
  "name": "gatsby-plugin-content-hash",
  "version": "0.0.3",
  "description": "Add content hash to requests for app-data.json and page-data.json generated by Gatsby.",
  "main": "index.js",
  "scripts": {
    "build": "tsc && mv TEMP_dist/*.js . && rm -rf TEMP_dist",
    "prepublish": "npm run build"
  },
  "keywords": [
    "gatsby",
    "plugin",
    "content hash"
  ],
  "author": "sititou70",
  "license": "MIT",
  "devDependencies": {
    "@types/fs-extra": "^9.0.11",
    "@types/glob": "^7.1.3",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "escape-string-regexp": "^4.0.0",
    "fs-extra": "^10.0.0",
    "glob": "^7.1.7"
  },
  "peerDependencies": {
    "gatsby": "^2.0.0 || ^3.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:sititou70/gatsby-plugin-content-hash.git"
  }
}
